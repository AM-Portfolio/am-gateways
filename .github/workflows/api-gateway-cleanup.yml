name: Container Cleanup — API Gateway

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Image tag to delete (e.g. sha-abc1234)"
        required: true
        type: string

jobs:
  cleanup:
    name: Delete am-gateways-api image tag — ${{ github.event.inputs.tag }}
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - name: Delete container image tag
        uses: actions/delete-package-versions@v5
        with:
          package-name: "am-gateways-api"
          package-type: "container"
          min-versions-to-keep: 5
          delete-only-pre-release-versions: "false"
